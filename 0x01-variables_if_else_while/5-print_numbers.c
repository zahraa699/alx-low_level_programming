#include <stdio.h>

/**
 * main - Entry point
 *
 * Description: prints number from 0 to 9
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
int num = 0;
for (num = 0; num < 10; num++)
{
printf("%i", num);
}
printf("\n");
return (0);
}
